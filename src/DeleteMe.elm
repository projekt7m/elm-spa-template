module DeleteMe exposing (ultimateAnswer)

-- This is a module, that serves the purpose of having something that can be
-- used in an example test.
-- Remove this, and test real code instead ...

ultimateAnswer : Int
ultimateAnswer = 42
